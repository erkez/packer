{
  "name": "packer-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "yarn@4.17.0",
  "workspaces": [
    "packages/*",
    "examples/*",
    "docs"
  ],
  "description": "Packer",
  "main": "src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/erkez/packer.git"
  },
  "scripts": {
    "build": "yarn workspace @ekz/packer build",
    "lint": "yarn workspace @ekz/packer lint && yarn workspace my-app lint",
    "docs:dev": "yarn workspace packer-docs start",
    "docs:build": "yarn workspace packer-docs build",
    "install:ci": "yarn install --immutable && yarn dedupe --check",
    "audit": "yarn npm audit -A -R --severity high",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "yarn build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8"
  },
  "license": "MIT",
  "engines": {
    "node": ">=24"
  }
}
