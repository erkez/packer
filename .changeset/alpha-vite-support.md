---
"@ekz/packer": minor
---

Add alpha Vite support alongside the existing Webpack API via `Packer.vite.createApplicationConfiguration()`. Includes Vite dependencies, a React + TypeScript Vite example app, Webpack-to-Vite migration guidance, expanded bundler-specific configuration docs, and refreshed Packer icon assets.
